{
  "name": "agents-catalog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "^24.15.0"
  },
  "scripts": {
    "build": "node scripts/build-catalog.mjs",
    "format": "prettier '**/*.md' --write",
    "format:check": "prettier '**/*.md' --check"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^5.2.1",
    "prettier": "^3.9.4"
  }
}
